name: Test flow

on:
  workflow_dispatch:

env:
  SOME_VAR: ${{ github.ref_type }}--${{ secrets.test }}

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo $SOME_VAR
